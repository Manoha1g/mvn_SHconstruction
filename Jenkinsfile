pipeline {
  agent any
  tools { 
    maven 'maven-3.6.1' 
  }
  stages{
    stage('creating a directory') {
      steps {
        echo "Creating a directory"
        sh "welcome"
      }
    }
  }
}
